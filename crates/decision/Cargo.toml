[package]
name = "decision"
description = "Pure state machine that decides when bosebridge should nudge the headphones"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
